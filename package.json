{
    "name": "uber-video",
    "version": "1.0.0",
    "description": "Uber Clone — full-stack monorepo",
    "scripts": {
        "dev": "concurrently --kill-others-on-fail -n BACKEND,FRONTEND -c cyan,magenta \"npm run dev --prefix Backend\" \"npm run dev --prefix frontend\"",
        "start": "concurrently --kill-others-on-fail -n BACKEND,FRONTEND -c cyan,magenta \"npm start --prefix Backend\" \"npm run preview --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}